require 'spec_helper'

describe Simplemvc do
  it 'has a version number' do
    expect(Simplemvc::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
